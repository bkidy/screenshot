{
  "name": "screenshot-service",
  "version": "1.0.0",
  "description": "轻量化的截图服务，基于Puppeteer和Express",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon src/app.js",
    "build": "chmod +x scripts/build.sh && ./scripts/build.sh",
    "deploy": "chmod +x scripts/deploy.sh && ./scripts/deploy.sh",
    "test": "curl -f http://localhost:3002/health"
  },
  "keywords": [
    "screenshot",
    "puppeteer",
    "docker",
    "microservice",
    "lightweight"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^24.10.0",
    "express-rate-limit": "^6.8.1",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 